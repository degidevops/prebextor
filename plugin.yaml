name: prebextor
version: 1.0.0
description: "Prebextor Deterministic Extraction Engine — Mapping → Pruning → Fetching → Cleaning → Markdown → Boundary XML Wrap → QA Gate. Built on CamoFox and markdownify."
author: degi
kind: backend
provides_web_providers:
  - prebextor
